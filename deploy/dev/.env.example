# Local development environment.
# Copy this file to deploy/dev/.env and adjust if needed.
# Do NOT use these settings in production.

DATA_ROOT=./deploy/dev/data
OAUTH_JWKS_URL=file://./deploy/dev/jwks.json
OAUTH_AUDIENCE=docroot-dev
OAUTH_ROLE_EXTRACTOR=keycloak
CORS_ORIGINS=http://localhost:3000
